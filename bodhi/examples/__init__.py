"""Runnable examples for the Bodhi SDK integrations.

Each module runs as a script, so nothing needs cloning:

    python -m bodhi.examples.pipecat_mic_bot
    python -m bodhi.examples.pipecat_stream_wav call.wav
"""
